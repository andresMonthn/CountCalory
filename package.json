{
  "name": "countcalory-root",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "mobile"
  ],
  "scripts": {
    "build": "echo 'Vercel will build client and server separately based on vercel.json'",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install && cd ../mobile && npm install"
  },
  "dependencies": {
    "nodemailer": "^7.0.12"
  }
}
